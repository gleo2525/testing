/// @desc without state machine

	//rolldice = keyboard_check(vk_space);
	//destroydice = keyboard_check_pressed(vk_space);
	//diceletgo = keyboard_check_released(vk_space);

	// Rolling and result
	//Since the below while loop doesn't account for the same number happening twice, this isn't a die. Needs tweaking
	//if (rolldice) {
	//	image_alpha = 0.5;
	//	image_index = irandom(5);
	//} 

	//if (diceletgo) {
	//	roll = image_index + 1;	
	//	o_player.points += roll;
	//	instance_destroy();
	//}
	